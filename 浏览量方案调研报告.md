# Hugo Stack 主题博客浏览量实现方案

## 调研背景

你的博客使用 Hugo + Stack 主题，已部署在 GitHub Pages，当前使用 giscus 作为评论系统。需要为文章添加浏览量显示功能。

---

## 五大方案对比

### 方案一：不蒜子 (Busuanzi) — ⭐ 推荐

| 维度 | 评价 |
|------|------|
| **费用** | 完全免费 |
| **注册** | 无需注册 |
| **部署** | 纯前端，只加几行 HTML/JS |
| **国内访问** | ✅ 快 |
| **稳定性** | ⚠️ 偶尔不稳定（官方服务器） |
| **数据可控** | ❌ 数据在第三方 |
| **GitHub 使用人数** | 🔥 最多（中文 Hugo 圈标配） |

**当前项目已初步实现，但显示效果需要优化。** 改进方向：
- 在文章列表页也能显示浏览量（目前只在文章内页显示）
- 样式优化，匹配主题风格
- 考虑自部署不蒜子后端提升稳定性

---

### 方案二：Waline 仅浏览量模式 — ⭐⭐ 强烈推荐

| 维度 | 评价 |
|------|------|
| **费用** | 免费（LeanCloud 开发版 + Vercel 免费额度） |
| **注册** | 需注册 LeanCloud + Vercel |
| **部署** | 中等（Vercel 部署后端，前端改模板） |
| **国内访问** | ✅ 快（Vercel 有香港节点） |
| **稳定性** | ✅ 好 |
| **数据可控** | ✅ 数据在 LeanCloud，可控可导出 |
| **GitHub 使用人数** | 🔥 很多（搜到 497 个仓库在用） |

**核心优点：可以只开启浏览量，关闭评论功能，不影响现有 giscus。**

```js
// Waline 仅浏览量模式
Waline.init({
    comment: false,       // 关闭评论
    pageview: true,       // 开启浏览量
    serverURL: "https://xxx.vercel.app"
})
```

模板中使用：`<span class="waline-pageview-count" data-path="{{ .RelPermalink }}"></span>`

Waline 自动扫描所有 `.waline-pageview-count` 元素并填入浏览量。

---

### 方案三：Cloudflare Workers + KV (pageview-worker)

| 维度 | 评价 |
|------|------|
| **费用** | 免费（CF Workers 免费额度 10万次/天） |
| **注册** | 需 Cloudflare 账号 |
| **部署** | 稍微复杂（需 wrangler CLI） |
| **国内访问** | ⚠️ 一般（CF 国内速度不如 Vercel） |
| **稳定性** | ✅ 极好 |
| **数据可控** | ✅ 完全自主 |

参考项目：https://github.com/jiacai2050/pageview-worker

---

### 方案四：Umami 自部署

| 维度 | 评价 |
|------|------|
| **费用** | 需服务器或 Vercel/ Railway 部署 |
| **注册** | 需部署数据库 |
| **功能** | 完整分析（PV/UV/来源/设备等），功能过剩 |
| **适用场景** | 需要完整网站分析的场景 |

---

### 方案五：GoatCounter

| 维度 | 评价 |
|------|------|
| **费用** | 免费（10万 PV/月） |
| **注册** | 需注册 |
| **国内访问** | ❌ 可能被墙或很慢 |
| **适用场景** | 面向海外读者的博客 |

---

## 推荐结论

**推荐方案二：Waline 仅浏览量模式**，理由如下：

1. **数据可靠**：LeanCloud 作为国内服务，访问稳定；数据可导出，不会丢失
2. **只做浏览量**：`comment: false` 关闭评论，不影响现有 giscus
3. **社区验证**：GitHub 上有 497+ 仓库使用 `waline-pageview-count`，大量 Hugo Stack 博主在用
4. **同时获得评论数统计**：可以顺带显示评论数（`waline-comment-count`），虽然评论实际由 giscus 处理
5. **美观成熟**：Waline 的页面浏览统计卡片设计精美，比不蒜子更现代

**备选方案一：不蒜子优化版**，如果不想注册任何服务，优化当前的不蒜子实现即可。

---

## 方案二具体实施步骤

### 第一步：注册配置 LeanCloud
1. 注册 [LeanCloud 国际版](https://console.leancloud.app/)
2. 创建应用（开发版免费）
3. 获取 AppID、AppKey、MasterKey

### 第二步：Vercel 部署 Waline 后端
1. 点击 [Vercel 一键部署](https://vercel.com/new/clone?repository-url=https://github.com/walinejs/waline/tree/main/example)
2. 配置环境变量：LEAN_ID、LEAN_KEY、LEAN_MASTER_KEY
3. 获得 serverURL（如 `https://waline-xxx.vercel.app`）

### 第三步：修改 Hugo 配置
`config/_default/params.toml` 添加 Waline 配置

### 第四步：修改模板文件
- `layouts/partials/article/components/details.html` — 文章列表页显示浏览量
- `layouts/partials/article/components/footer.html` — 文章内页显示浏览量
- 添加 Waline JS 脚本加载

### 第五步：部署
推送代码，GitHub Actions 自动发布

---

## 各方案文件修改量对比

| 方案 | 需修改文件数 | 需注册服务 | 维护成本 |
|------|------------|-----------|---------|
| 不蒜子优化 | 3-4 | 无 | 低 |
| Waline 仅浏览 | 4-5 | LeanCloud+Vercel | 中 |
| CF Workers | 3-4 | Cloudflare | 中 |
| Umami | 4-5 | 服务器+数据库 | 高 |
| GoatCounter | 2-3 | GoatCounter | 低 |
