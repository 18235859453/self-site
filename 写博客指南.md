# 写博客指南

## 文件结构

每篇文章是一个**文件夹**，放在 `content/post/` 下，Markdown 文件必须命名为 `index.md`：

```
content/post/my-article/
├── index.md          # 文章内容
├── cover.jpg         # 封面图（可选）
└── demo.png          # 文中插图
```

## Front Matter（前置参数）

在 `index.md` 顶部填写：

```yaml
---
title: "文章标题"
description: "文章简介"
date: 2026-05-27
slug: "custom-url"         # 自定义 URL，可选
image: "cover.jpg"         # 封面图，可选
categories:
  - 技术
tags:
  - Hugo
  - 博客
draft: false               # true=草稿不发布，false=发布
toc: true                  # 显示目录，默认 true
comments: true             # 显示评论区，默认 true
---
```

## 图片引用

**单张图片：**

```markdown
![](图片名.jpg)
```

**相册画廊（同一行多张图，中间用两个空格隔开）：**

```markdown
![](img1.jpg)  ![](img2.jpg)
![](img3.jpg)  ![](img4.jpg)
```

主题会自动拼成可点击放大的画廊。

## 发布流程

```bash
# 1. 本地预览
hugo server -D

# 2. 确认无误后提交
git add .
git commit -m "新文章：xxx"

# 3. 推送到 GitHub（自动部署）
git push origin main
```

推送后 GitHub Actions 自动构建部署，一两分钟后在 `https://18235859453.github.io/self-site/` 即可看到。
